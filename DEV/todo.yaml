explorer.html:
    allow downloading folders in addition to files
    auto-detect bcsv file based on content (some bcsv files have no extension, no bcsv files have magic at start)

    add file viewers
        text
        json
        csv
        hex
converters:
    fix:
        .byml:
            arrays incorrectlycontain all null items

    allow resaving same Nintendo file type:
        choose little or big endian
        does not allow importing new file (too complicated for now)
        eventually, allow converting common file types to Nintendo file types
